spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ontology_platform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: root
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: neo4j123
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.ontology.platform: DEBUG
